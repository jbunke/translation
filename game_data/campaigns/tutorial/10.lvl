name:{Save & Load Position}
hint:{Save position with key:<SAVE_POS> and load position with key:<LOAD_POS>. Loading preserves your vertical velocity.}
stats:{}
platforms:{
	(0,0,200);
	(0,-400,200)
}
sentries:{
	(PUSHER,1,-6)
}
